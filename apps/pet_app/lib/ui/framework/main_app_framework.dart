// TODO: 重构以使用新的 communication_system 模块
import 'package:flutter/material.dart';

class MainAppFramework extends StatelessWidget {
  const MainAppFramework({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MainAppFramework - 等待重构'),
    );
  }
}
